# 基于Java的家居销售系统的设计与实现

## 摘要

随着互联网电商的普及，传统家居行业正在经历从线下到线上的深刻转型。本文围绕家居销售的实际业务需求，设计并开发了一套完整的家居销售系统。系统后端以Spring Boot为核心框架，数据持久层采用MyBatis，前端选用Vue.js结合Element UI组件库，数据库使用MySQL。整个项目的功能点覆盖了用户端和管理员端两大部分，总计实现了58项具体功能，包括商品展示与搜索、购物车管理、订单处理、模拟支付、商品评价、收藏夹、在线客服等常见电商模块，以及数据仪表盘、报表导出等运营管理工具。系统还内置了一个融合推荐引擎，通过协同过滤和基于内容的混合策略，结合用户浏览与购买行为的采集分析，实现了不同用户的个性化商品推送。系统向外提供102个REST接口，数据库设计了18张表，代码总量接近两万四千行。经过36个自动化用例和完整业务流程的测试，系统运行稳定，基本达到了预期的设计目标。

**关键词**：家居销售；Spring Boot；Vue.js；混合推荐；前后端分离

## Abstract

This thesis presents a furniture sales system built with Java, adopting a front-end and back-end separation architecture. The back-end relies on Spring Boot and MyBatis, while the front-end uses Vue.js with Element UI. MySQL serves as the database. The system delivers 58 features across both customer and administrator roles, covering core e-commerce workflows including product browsing, cart operations, order management, simulated payment, reviews, favorites, and customer service chat. A hybrid recommendation engine combining collaborative filtering and content-based approaches was implemented to provide personalized product suggestions based on user behavior. The back-end exposes 102 REST APIs, the database contains 18 tables, and the total codebase reaches approximately 24,000 lines. Testing results from 36 automated cases and full business-flow verification confirm the system meets functional requirements with acceptable stability.

**Keywords**: Furniture Sales; Spring Boot; Vue.js; Hybrid Recommendation; Front-end Separation

---

## 第一章 绪论

### 1.1 研究背景

国内家居市场在过去十年间保持了持续的增长态势。根据行业统计数据，家居类商品的线上渗透率逐年走高，越来越多的消费者习惯通过手机或电脑浏览、挑选、下单购买家居产品。这种消费习惯的迁移直接推动了一大批家居电商平台的出现和成长。

然而，中小型的家居商家在数字化转型过程中仍然面临不少困难。自建电商系统成本高、周期长，而入驻大型平台又面临激烈的同质化竞争和高昂的平台费用。因此，开发一套功能相对完整、部署成本可控、并且能够快速上线的家居销售系统，对于中小型商家来说具有比较实际的价值。

从技术层面来看，近年来前后端分离的开发模式已经非常成熟。Spring Boot大幅降低了Java Web项目的搭建门槛，Vue.js等前端框架也让交互式单页应用的开发效率提升了不少。这些技术的普及，使得独立开发一个中等规模的电商系统成为了可能。

### 1.2 研究意义

做这个选题的出发点比较明确——就是把课堂上学到的Spring Boot、MyBatis和Vue.js这些技术，真正用到实际项目里去，完成一个能跑起来、功能相对完整的系统。整个开发过程中踩过的坑和积累的经验，对于理解企业级Java开发的完整流程还是挺有帮助的。

从系统本身的功能来看，它基本覆盖了日常电商场景需要用到的各个环节。普通用户可以在系统上浏览商品、加购、下单、支付、评价、联系客服，管理员可以通过后台面板管理商品、订单、用户、活动数据，并且看到可视化的统计图表。这些功能组合在一起，已经能够支撑起一个中小规模家居店铺的线上运营。

另外在推荐算法方面，本系统没有直接套用现成的推荐框架，而是自己动手实现了一套比较轻量的混合推荐逻辑。虽然算法本身不算复杂，但从数据采集、用户画像构建，到协同过滤和内容匹配的完整流程都走了一遍，对理解推荐系统的工作原理很有帮助。

### 1.3 国内外研究现状

电商系统的技术方案经过多年的演进已经比较成熟了。在国外，Amazon、eBay等平台很早就开始使用微服务架构和个性化推荐技术。Netflix的推荐系统更是业界标杆，从早期的协同过滤一路演进到深度学习模型。国内方面，淘宝、京东等平台的技术架构同样非常先进，但相应的技术门槛和成本也极高，对中小型项目来说参考意义有限。

在开源社区中，基于Spring Boot的电商项目模板非常多，但大多数只是实现了最基础的CRUD功能，缺乏推荐算法、数据统计、报表导出等相对深入的功能模块。本系统在参考这些开源项目的基础上，补充了推荐引擎、ECharts数据可视化和EasyExcel报表导出等模块，希望能够为类似的项目提供一个功能相对完整的参考案例。

### 1.4 论文结构安排

本文总共分为七章。第一章是绪论，交代背景和动机。第二章简单梳理了项目中用到的几项关键技术。第三章从功能和非功能两个角度做了需求分析。第四章是系统设计，重点讲了架构、数据库和接口的设计思路。第五章选取了几个关键模块描述了实现细节。第六章用自动化测试和手动测试两条线验证了系统的功能。第七章做了一个总结，也谈了谈后续可以改进的地方。

---

## 第二章 相关技术介绍

### 2.1 Spring Boot

Spring Boot是Pivotal团队在Spring框架基础上推出的一个快速开发脚手架。它的核心设计理念是"约定优于配置"——通过自动配置和起步依赖，让开发者不需要在XML配置文件中反复折腾，就可以快速搭建起一个可运行的Web应用。

本系统使用了Spring Boot的几个关键特性。首先是内嵌的Tomcat容器，应用打包成jar后直接执行就能启动，省去了单独安装配置Web服务器的步骤。其次是起步依赖，像`spring-boot-starter-web`集成了Spring MVC和Jackson，`spring-boot-starter-security`引入了安全框架的支持。项目里还用了`@CrossOrigin`注解和自定义的CorsConfig来处理前后端分离情况下的跨域请求。

### 2.2 MyBatis

MyBatis是Java生态中一个半自动化的ORM框架。和Hibernate那种全自动映射不同，MyBatis允许开发者自己编写SQL，只是在Java方法和SQL语句之间建立映射关系。这样做的好处是对SQL拥有完全的控制权，遇到复杂查询或性能调优的时候更灵活。

在项目中，所有Mapper接口对应一个同名的XML映射文件，动态SQL通过`<if>`、`<foreach>`等标签实现。配置文件里开启了`map-underscore-to-camel-case`，这样数据库里的`created_at`字段就能自动映射到Java对象的`createdAt`属性，少写了不少重复的映射代码。

### 2.3 Vue.js与Element UI

Vue.js是一个轻量级的JavaScript框架，它的核心是响应式数据绑定和组件化开发。和React、Angular相比，Vue的上手曲线更平缓，文档对中文的支持也比较好。

Element UI则是饿了么团队开源的一套Vue组件库，提供了按钮、表格、表单、弹窗、导航菜单等常用界面组件。项目里管理后台的表格、对话框、筛选下拉框、分页组件基本都是直接用Element UI现成的组件拼出来的，省去了大量手写CSS和交互逻辑的工作。

### 2.4 其他技术

**MySQL**作为数据库，项目使用了InnoDB引擎以支持事务和外键约束。**JWT**（JSON Web Token）用来做无状态认证，避免了服务端维护Session的开销。**ECharts**在管理后台的数据统计页面中用了折线图和饼图来展示销售趋势和状态分布。**EasyExcel**是阿里开源的一个Excel读写工具，导出报表的功能就是基于它实现的。**BCrypt**用来加密用户密码，即便数据库泄露也无法还原明文。

---

## 第三章 系统需求分析

### 3.1 用户角色

系统设定了两种角色：普通用户（USER）和管理员（ADMIN）。普通用户是系统的消费者端使用者，可以浏览商品、管理购物车、下单支付、评价收藏等。管理员通过专门的后台面板进行商品上下架、订单处理、用户管理、数据查看等操作。

角色区分通过数据库中的`role`字段实现，前端路由守卫读取sessionStorage中的角色信息来判断是否有权限访问管理后台。

### 3.2 用户端功能需求

用户端承担了消费者与系统交互的全部功能，按照业务流程梳理如下：

首先是**账户相关**的功能，包括注册、登录、个人资料修改和密码修改。用户注册时需要提供用户名和密码，也可以补充手机号和邮箱。登录成功后系统返回一个JWT令牌，前端把它存到sessionStorage里，每次请求自动带上。

**商品浏览**方面，首页聚合了轮播图、分类导航、推荐商品、热门商品、销量排行榜和最新评价六个板块。商品列表页支持按分类筛选和分页查看。搜索功能通过后端拼接LIKE条件实现模糊匹配。商品详情页展示了商品的图片、价格、规格参数、库存、销量和用户评价。

**购物车**是用户下单的前置环节。用户可以随时把商品加入购物车，在购物车页面调整数量、删除单件商品，或者一键清空。选中商品后点击结算按钮，会进入订单确认页面。

**订单流程**从确认页面开始：用户需要选择一个收货地址和支付方式，确认金额无误后提交订单。系统生成一个UUID作为订单号，订单初始状态为待支付。跳转到支付页面后，用户选择支付宝、微信或银行卡（模拟支付），点击确认支付完成付款。支付成功后显示支付成功页面，5秒倒计时自动跳到订单列表。

用户在订单列表可以按状态筛选订单，待支付和已支付的订单可以取消，已发货的可以确认收货，已完成的可以去评价。

**评价与收藏**：确认收货后的订单可以进行评价，评价包括1到5星的评分和文字内容。用户可以管理自己写过评价，也可以把喜欢的商品加入收藏夹。

**客服功能**：用户右下角的浮动客服按钮点击后会弹出一个聊天窗口，可以和系统管理员实时发送消息。

### 3.3 管理员端功能需求

管理员端的所有功能都集中在Admin.vue这个单文件组件中，通过左侧的垂直菜单切换不同的管理模块。

**商品管理**是使用频率最高的模块。管理员可以查看所有商品的列表（支持按分类筛选和分页），点击添加按钮弹出一个表单对话框，填写名称、价格、库存、分类、描述、尺寸等信息并上传图片后提交。编辑和删除操作都在表格的操作列中完成，删除操作有二次确认对话框。

**订单管理**同样重要。管理员可以看到所有用户的订单，按状态下拉筛选（待支付/已支付/已发货/已完成/已取消），也可以点击某个订单查看详情或编辑订单信息。对于已支付但未发货的订单，有"发货"按钮；已发货的有"完成"按钮；未支付的可以直接取消。

**用户管理**、**分类管理**、**轮播图管理**和**活动管理**的模式类似——都是列表展示加上增删改查的操作界面。活动的管理稍微复杂一些，因为活动下面还需要关联具体的商品和活动价。

**数据统计**分为两个页面。仪表盘页面展示了销售额、订单数、商品数、用户数四个指标卡，以及用户统计、分类统计、订单概况、销售趋势图和状态分布图。数据统计页面提供了更详细的销售和订单数据，以及近7天/30天的订单趋势、分类销量占比和评分分布四个图表。页面顶部还有一个导出工具栏，可以按状态和日期筛选后导出订单Excel报表，也可以直接导出销售统计和商品统计报表。

**客服管理**页面左侧是发送过消息的用户列表（带未读消息数红点），右侧是聊天区域，管理员可以查看聊天记录、回复消息、标记已读。

### 3.4 非功能需求

系统在工作过程中需要满足一些非功能性的要求。安全性方面，密码通过BCrypt加密，前端发来的每个请求都需要经过JWT过滤器验证，未登录的请求会被Spring Security拦截。可用性方面，各个数据加载环节都加了v-loading加载动画，列表为空时有el-empty空状态提示，操作失败有el-message错误提示。可维护性方面，后端严格按照Controller→Service→Mapper的分层结构编写，前端将API调用统一封装在api/目录下的各模块文件中。

---

## 第四章 系统设计

### 4.1 整体架构

系统采用了经典的B/S三层架构加上前后端分离的部署方式。用户的浏览器加载Vue.js打包后的静态文件，页面通过Axios向Spring Boot后端发送异步请求。后端接收到请求后，由对应的Controller方法处理，调用Service层执行具体的业务逻辑，Service层再通过Mapper层操作数据库。响应数据统一用自定义的Result类包装后返回JSON。

### 4.2 模块划分

整个系统按照功能领域分成了十四个模块：用户模块（M1）、商品模块（M2）、购物车模块（M3）、订单模块（M4）、支付模块（M5）、地址模块（M6）、评价模块（M7）、收藏模块（M8）、客服模块（M9）、轮播图模块（M10）、活动模块（M11）、推荐模块（M12）、统计模块（M13）和定时任务模块（M14）。

每个模块在后端有对应的Controller和Service，在前端有对应的Vue页面或组件。模块之间通过REST API进行通信，数据库外键保证关联数据的完整性。

### 4.3 数据库设计

数据库一共建了18张表，其中核心的用户表、商品表、订单表和订单明细表形成了业务主链路。

用户表（user）有12个字段，username做了唯一索引。密码字段存的是BCrypt加密后的密文，长度设为255。role字段区分USER和ADMIN两种角色。

商品表（product）有17个字段，是字段最多的一张表。price和original_price用DECIMAL(10,2)存金额避免浮点精度问题。sales字段记录累计销量，rating字段记录平均评分。image存主图URL，images存多图JSON。category_id和brand_id分别外键关联到分类表和品牌表。

订单表（orders）的主键用的是32位的UUID字符串，不是自增整数，这样可以避免订单号被猜出来。status字段用数字编码（0待付/1已付/2已发/3完成/4取消）。payment_method、payment_time、shipping_address等字段记录支付和收货信息。

订单明细表（order_item）关联了订单和商品，把下单时的商品名称、图片、单价做了快照存下来，避免后续商品信息修改影响历史订单的准确性。

其他表如购物车表、地址表、评价表、轮播图表、活动表等结构都比较常规，就不一一展开了。

### 4.4 接口规范

后端对外暴露了102个REST接口。所有接口的响应都遵循同一个格式——外层是`success`、`message`、`data`三个字段，前端判断success是否为true来决定下一步处理。分页查询的data里会多一层list和total。

```

---
## 第五章 系统实现

### 5.1 推荐算法的设计与实现

推荐算法这个模块是本系统里花了比较多时间的一个部分，前后写了大概530行Java代码，主要集中在RecommendationServiceImpl这个类里。

#### 5.1.1 整体思路

算法的核心思路是把三种策略混合起来用。首先尝试协同过滤——找到和当前用户购买偏好相似的其他用户，把他们的购买记录里当前用户没买过的商品推荐出来。如果协同过滤能推荐的数量不够，就再跑基于内容的推荐——针对当前用户的历史浏览和购买，分析出他喜欢什么分类、什么价位，然后给每个商品打个匹配分。如果还不够，最后多退少补，拿销量和评分都不错的商品把数量补满。

#### 5.1.2 行为数据采集

用户行为数据的采集是实时做的。当用户打开商品详情页的时候，系统会自动调用`recordUserView`接口，把userId和productId记录到user_behavior表里。当用户支付成功后，Payment.vue会遍历订单里的每一个商品，逐一调用`recordUserPurchase`接口记录购买行为。

两处采集都是双写——同时写到MySQL和内存中的HashMap。数据库用来持久化，保证服务重启后数据不丢；内存用来快速查询，避免每次计算推荐都去扫全表。

#### 5.1.3 用户画像构建

用户画像是从行为数据中提炼出来的偏好模型，主要采集两个维度：分类偏好和价格偏好。

分类偏好就是用Map统计用户在不同分类上的浏览次数和购买次数，分别乘上对应的权重（浏览0.1，购买0.5）累加起来。比如用户看了3次客厅家居的商品，买了1次，那么客厅家居的偏好分就是3×0.1+1×0.5=0.8。

价格偏好做了类似的处理，只是把连续的价格做了一下离散化——每1000块钱划一档。用户在哪个价格区间的行为多，就说明他偏好这个价位。

#### 5.1.4 协同过滤

协同过滤部分用的是最简单的User-based思路。先把当前用户的所有购买商品放到一个Set里，然后遍历其他用户，同样取出他们的购买Set。用Jaccard相似度公式计算两个用户的重叠程度，公式是：交集的大小除以并集的大小。相似度大于0.1的用户会被保留下来。

对于每个保留下来的相似用户，把他们买过但当前用户没买过的商品列出来，该商品的推荐得分等于所有相似用户的相似度之和。最后按得分排序，取top N。

#### 5.1.5 基于内容的推荐

这一层的输入是前面构建好的用户画像，输出是对每个待推荐商品的匹配分数。算分的公式是：分类偏好分×0.6 + 价格偏好分×0.3 + 商品评分归一化×0.1。

如果用户从未浏览过这个商品的分类，那分类偏好分就用一个很小的默认值0.05，而不是0.5。这里有一个之前踩过的坑——默认值设成0.5的时候，没看过的分类反而比看过但权重低的分类得分高，结果推荐的居然是用户完全没接触过的类目。后来把默认值调成0.05才纠正了这个问题。

#### 5.1.6 整体流程串联

拿到一个userId后的执行顺序是：先从数据库加载行为数据到内存→接着计算用户所有分类偏好的总和，如果太低（<0.15）说明这个用户行为数据还不够，直接返回热门商品→够的话就构建用户画像→跑协同过滤→跑内容推荐→还不够就用销量×0.5+评分×0.5排序的兜底策略补齐数量→最后去重排序返回结果。

### 5.2 订单状态流转

订单的五个状态（待支付→已支付→已发货→已完成，任何一步都可能跳到已取消）之间的流转是订单模块的核心逻辑。当用户在前端点击取消按钮，或者到了支付超时由定时器自动取消时，后端不仅要更新订单状态，还要把之前扣掉的库存恢复回来，同时回退销量计数器。

库存和销量的恢复逻辑写在OrderServiceImpl的updateStatus方法里。方法会先查一下订单的当前状态，如果是从非取消变成取消，就遍历订单明细，对每个商品调increaseStock和increaseSales（销量传负数实现减扣）。如果反过来从取消恢复成其他状态，就把库存再扣掉、销量再加回来。

系统还写了一个定时任务OrderScheduler，用@Scheduled注解配置每小时执行一次。它的逻辑是找出所有状态为待支付且创建时间超过2小时的订单，逐个调updateStatus把它们批量取消。

### 5.3 数据统计页面

管理后台的仪表盘页面把一些关键的运营数据聚合到了一起。最上面是一排四个指标卡——总销售额、总订单数、总商品总数和注册用户总数。下面还有用户统计和分类统计的卡片，以及一个订单概况（待支付/已支付/已完成各多少单）。接着是两个ECharts图表，分别展示最近的销售趋势和订单状态分布。最下面是热门商品的TOP5排行和最近的五笔订单。

数据统计页面则更加细节化一些。开头是一个导出工具栏，可以按条件导出订单Excel报表。下面是销售额的三张卡和订单数的四张卡。再往下有四个图表——7天订单趋势线、30天订单趋势线、分类销量占比饼图和评分分布柱状图。分类销量这块的一个小坑是，起初计算逻辑只统计状态为"已完成"的订单，后来改成了直接汇总product表的sales字段，保证了前后台显示的销量数字完全吻合。

### 5.4 前后端交互说明

前后端的通信全部走的是HTTP异步请求。前端把所有和后端打交道的逻辑都抽到了api/目录下的文件里，Vue页面只负责调这些函数和处理响应数据。请求的发送和响应的拦截统一在utils/request.js这个文件里做了封装。

这个封装文件做了几件事：一是给每个请求自动加cancelToken，方便在页面切换的时候取消还在pending的请求；二是通过请求拦截器从sessionStorage里取出token，塞到Header的Authorization字段里；三是通过响应拦截器把后端返回的axios Response对象解开一层，直接拿到里面的data。

前端发起请求用的是`Promise.allSettled`来并行请求，这样一个接口超时或者报错了不会影响其他接口的数据展示。返回的数据在各自的方法里做了一层兼容处理，兼顾到不同后端版本的返回格式差异。

---

## 第六章 系统测试

### 6.1 测试方法

测试这块一共做了两种。一种是用Python写的自动化接口测试，覆盖了16个功能模块共36个测试用例。另一种是手动操作，把几个核心业务流程完整地走了一遍。

### 6.2 自动化API测试

测试脚本用Python 3.8加requests库写的，放在项目的tests目录下面。36个用例覆盖了系统的全部公开接口，从最基础的服务器能不能连通，到用户注册登录、商品增删改查、购物车和订单操作、收藏和评价、客服消息、推荐算法的接口等等。

每个用例就是一个独立的函数，先发送HTTP请求，然后对返回的状态码和响应体结构做断言。断言通过就打印PASS，失败就打印FAIL并带上具体的报错信息。最后统计算一下通过率。目前的测试结果是36个用例全部通过。

### 6.3 手动流程测试

手动测试重点跑了下面这几条路径：

第一条是注册登录流，从注册新账户到登录，再到退出清session。

第二条是购物下单流。在首页挑一个推荐商品点开详情，加入购物车，在购物车页面调整数量，选中后结算，选一个已有的收货地址，确认订单，在支付页选微信支付，完成支付后看到对勾动画和倒计时跳转。

第三条是订单管理流。在订单列表筛选已支付状态，找到刚下的单，看看详情页的信息对不对。然后切到管理员账号，进管理后台，把这条订单从已支付改成已发货状态。

第四条是评价收藏流。回到用户账号，确认收货后再去评价，打个4分写几句话。然后把商品加入收藏夹，在收藏页检查能不能看到。

第五条是管理后台数据流。确认仪表盘的数据卡片和统计图表能正常加载，导出一个订单报表检查Excel里的内容对不对。

这几条路径跑下来，功能链路都是通的，没有遇到阻断性的问题。偶尔遇到的一些界面小毛病比如图片加载失败、筛选后分页没重置到第一页等问题也都在开发过程中逐个修掉了。

### 6.4 测试结论

经过API自动化和手动流程两轮验证，系统各项功能均已达到预期，前后端数据交互正常，业务流程无阻塞性缺陷。

---

## 第七章 总结与展望

### 7.1 工作总结

这个家居销售系统从需求分析到最终上线测试，前后花了不少时间和精力。现在回过头来看，确实完成了当初设想的大部分功能。用户端跑通了从注册、浏览商品、加购、下单、支付到评价和收藏的完整交易闭环。管理后台也把商品管理、订单处理、用户管理、数据统计、报表导出这些运营常用的功能都做进去了。

推荐算法这块花了比较多的心血。虽然最后实现出来的算法复杂度算不上多高，但从零开始搭数据采集管道、构建用户画像、调权重参数、把三层策略串起来，这个过程对理解推荐系统的工作原理帮助还是挺大的。过程中也踩了一些坑，比如默认偏好值设太高反而降权的问题，比如行为数据加载顺序不同步导致首轮推荐结果为空的问题，都是实际调试中发现的。

代码总量大概两万四千行，前后端各占一半左右。数据库建了18张表，提供的API接口有102个。能独立完成这么一个中等规模的项目，在技术和工程能力上都有不小的收获。

### 7.2 不足之处

尽管功能基本都跑通了，但客观来讲，系统还是有一些明显的短板。

第一个是支付目前只是模拟的。点确认支付之后系统直接就把订单状态改了，没有对接任何第三方支付平台，也没有处理退款和回调这些现实中很复杂的场景。如果真的要上线用，这块肯定得重新做。

第二个是推荐算法的性能问题。目前所有计算都是在一个HashMap里完成的，数据量大了肯定扛不住。而且服务一重启内存里的数据就全丢了，得靠数据库重新加载，这个过程在用户量大之后会越来越慢。后面如果要优化，可以考虑用Redis做中间缓存层。

第三个是测试不够深入。现在只有API层面的黑盒测试，后端业务代码里没有任何单元测试。像订单创建、库存扣减、销量回退这些比较复杂的逻辑，其实很需要单元测试来保证正确性。

第四个是移动端的适配做得一般。虽然用了响应式布局，但在手机上的体验和PC端比差距还是有点大，尤其是管理后台那些表格和图表在窄屏上显示效果不太好。

### 7.3 未来改进方向

接下来如果有机会继续完善，我觉得可以从这几方面入手。一是把支付部分换成真实的支付宝或微信支付SDK，把退款和对账的流程也补上。二是给推荐系统引入Redis缓存，把画像数据存在Redis里，避免服务重启后数据丢失，也方便扩展到多实例部署。三是写后端的单元测试，至少把订单和库存相关的复杂业务逻辑用JUnit和Mockito覆盖到。四是针对手机端做一个更精细的适配，或者干脆用uni-app之类的框架单独出一版移动端。

总的来说，这个项目作为一个毕业设计，在功能完整度和技术深度上都基本达到了预先设想的要求。通过这个项目也确实把前后端分离开发、数据库设计、推荐算法实现、测试用例编写这一整套流程从头到尾走了一遍，收获挺大的。

---

## 参考文献

[1] 李刚. 疯狂Java讲义(第5版)[M]. 北京: 电子工业出版社, 2019.

[2] Evan You. Vue.js Guide[EB/OL]. https://vuejs.org/guide/, 2025.

[3] Spring Team. Spring Boot Documentation[EB/OL]. https://docs.spring.io/spring-boot/docs/current/reference/, 2025.

[4] MyBatis Team. MyBatis 3 User Guide[EB/OL]. https://mybatis.org/mybatis-3/, 2025.

[5] 王珊, 萨师煊. 数据库系统概论(第5版)[M]. 北京: 高等教育出版社, 2014.

[6] Ricci F, Rokach L, Shapira B. Recommender Systems Handbook(3rd ed.)[M]. New York: Springer, 2022.

[7] 项亮. 推荐系统实践[M]. 北京: 人民邮电出版社, 2012.

[8] 萨师煊, 王珊. 数据库系统概论(第4版)[M]. 北京: 高等教育出版社, 2006.

[9] Apache ECharts Team. ECharts Documentation[EB/OL]. https://echarts.apache.org/en/index.html, 2025.

[10] Element Plus Team. Element UI Documentation[EB/OL]. https://element.eleme.cn/, 2025.

[11] Oracle. MySQL 8.0 Reference Manual[EB/OL]. https://dev.mysql.com/doc/refman/8.0/en/, 2025.

[12] Jones M, Bradley J, Sakimura N. JSON Web Token (JWT)[S]. IETF RFC 7519, 2015.

[13] Freeman E, Robson E. Head First Design Patterns(2nd ed.)[M]. O'Reilly Media, 2020.

[14] Martin R C. Clean Code: A Handbook of Agile Software Craftsmanship[M]. Prentice Hall, 2008.

[15] 阿里巴巴. EasyExcel[EB/OL]. https://easyexcel.opensource.alibaba.com/, 2025.
