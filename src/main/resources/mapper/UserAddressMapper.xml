<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.furniture.mapper.UserAddressMapper">
    <!-- 根据用户ID查询地址列表 -->
    <select id="findByUserId" resultType="com.furniture.entity.UserAddress">
        SELECT * FROM user_address WHERE user_id = #{userId} ORDER BY is_default DESC, created_at DESC
    </select>
    
    <!-- 根据ID查询地址 -->
    <select id="findById" resultType="com.furniture.entity.UserAddress">
        SELECT * FROM user_address WHERE id = #{id}
    </select>
    
    <!-- 插入地址 -->
    <insert id="insert">
        INSERT INTO user_address (user_id, name, phone, province, city, district, detail_address, is_default, created_at, updated_at)
        VALUES (#{userId}, #{name}, #{phone}, #{province}, #{city}, #{district}, #{detailAddress}, #{isDefault}, #{createdAt}, #{updatedAt})
    </insert>
    
    <!-- 更新地址 -->
    <update id="update">
        UPDATE user_address
        SET name = #{name}, phone = #{phone}, province = #{province}, city = #{city},
            district = #{district}, detail_address = #{detailAddress}, is_default = #{isDefault}, updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>
    
    <!-- 删除地址 -->
    <delete id="delete">
        DELETE FROM user_address WHERE id = #{id}
    </delete>
    
    <!-- 重置用户的所有地址为非默认 -->
    <update id="resetDefault">
        UPDATE user_address SET is_default = 0 WHERE user_id = #{userId}
    </update>
    
    <!-- 设置地址为默认 -->
    <update id="setDefault">
        UPDATE user_address SET is_default = 1 WHERE id = #{id}
    </update>
</mapper>